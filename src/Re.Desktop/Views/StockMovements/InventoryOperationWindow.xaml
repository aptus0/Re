<Window x:Class="Re.Desktop.Views.StockMovements.InventoryOperationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Inventory Operation Center" Width="800" Height="640" MinWidth="800" MinHeight="640"
        WindowStartupLocation="CenterOwner" WindowStyle="None" AllowsTransparency="False"
        Background="#F8FAFC" BorderBrush="#CBD5E1" BorderThickness="1">
    
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolVisibility"/>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#1E293B"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Margin" Value="0,4,0,12"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Margin" Value="0,4,0,12"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/> <!-- Custom Title Bar -->
            <RowDefinition Height="*"/>    <!-- Scrollable Content -->
            <RowDefinition Height="54"/>   <!-- Actions Bar -->
        </Grid.RowDefinitions>

        <!-- Drag Header -->
        <Border Grid.Row="0" Background="#032D60" MouseDown="TitleBar_MouseDown" Padding="16,0">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="📦" FontSize="14" Foreground="White" Margin="0,0,8,0"/>
                    <TextBlock Text="Inventory Operation Center" Foreground="White" FontSize="14" FontWeight="Bold"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Border Background="#1E293B" CornerRadius="10" Padding="8,2" Margin="0,0,12,0">
                        <TextBlock Text="AUDIT-READY" Foreground="White" FontSize="9.5" FontWeight="Bold"/>
                    </Border>
                    <Button Content="×" Background="Transparent" Foreground="White" BorderThickness="0" FontSize="18" Padding="0" Width="30" Height="30" Cursor="Hand" Click="Cancel_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Form Scroll Area -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Border Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="24" Margin="20">
                <StackPanel>
                    <TextBlock Text="Operation Settings" FontSize="15" FontWeight="Bold" Foreground="#0F172A" Margin="0,0,0,14"/>

                    <TextBlock Text="Operation Type *" FontWeight="SemiBold"/>
                    <ComboBox ItemsSource="{Binding OperationTypes}" SelectedItem="{Binding SelectedOperationType}"/>

                    <TextBlock Text="Product *" FontWeight="SemiBold"/>
                    <ComboBox ItemsSource="{Binding Products}" SelectedItem="{Binding SelectedProduct}" DisplayMemberPath="Name"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="14"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Source Warehouse *" FontWeight="SemiBold"/>
                            <ComboBox ItemsSource="{Binding Warehouses}" SelectedItem="{Binding SourceWarehouse}" DisplayMemberPath="Name"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2" Visibility="{Binding IsTransfer, Converter={StaticResource BoolVisibility}}">
                            <TextBlock Text="Destination Warehouse *" FontWeight="SemiBold"/>
                            <ComboBox ItemsSource="{Binding Warehouses}" SelectedItem="{Binding DestinationWarehouse}" DisplayMemberPath="Name"/>
                        </StackPanel>
                    </Grid>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="14"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="14"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="{Binding QuantityLabel}" FontWeight="SemiBold"/>
                            <TextBox Text="{Binding Quantity}"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Unit Cost (TL)"/>
                            <TextBox Text="{Binding UnitCost}"/>
                        </StackPanel>
                        <StackPanel Grid.Column="4">
                            <TextBlock Text="Variant"/>
                            <ComboBox ItemsSource="{Binding Variants}" SelectedItem="{Binding SelectedVariant}" DisplayMemberPath="Code"/>
                        </StackPanel>
                    </Grid>

                    <Border Background="#F0F9FF" BorderBrush="#B9E6FE" BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,4,0,16">
                        <StackPanel>
                            <TextBlock Text="Traceability Attributes" FontWeight="Bold" Foreground="#014486" Margin="0,0,0,12"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="12"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="12"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Lot Number"/>
                                    <TextBox Text="{Binding LotNumber}" Margin="0,4,0,0"/>
                                </StackPanel>
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="Serial Number"/>
                                    <TextBox Text="{Binding SerialNumber}" Margin="0,4,0,0"/>
                                </StackPanel>
                                <StackPanel Grid.Column="4">
                                    <TextBlock Text="Expiry Date"/>
                                    <DatePicker SelectedDate="{Binding ExpiryDate}" Height="32" Margin="0,4,0,0"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="14"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Reference Document / No"/>
                            <TextBox Text="{Binding ReferenceNumber}"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Reason for Operation *" FontWeight="SemiBold"/>
                            <TextBox Text="{Binding Reason}"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>
        </ScrollViewer>

        <!-- Footer Actions -->
        <Border Grid.Row="2" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="0,1,0,0" Padding="16,10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Discard" Click="Cancel_Click" Background="White" Foreground="#475569" BorderBrush="#CBD5E1" BorderThickness="1" Margin="0,0,8,0" Padding="20,0" Height="34" FontWeight="SemiBold" Cursor="Hand">
                    <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="6"/></Style></Button.Resources>
                </Button>
                <Button Content="Post Operation" Command="{Binding SaveCommand}" Background="#0070D2" Foreground="White" BorderThickness="0" Padding="24,0" Height="34" FontWeight="Bold" Cursor="Hand">
                    <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="6"/></Style></Button.Resources>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>

<Window x:Class="Re.Desktop.Views.StockMovements.StockAdjustmentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Stock Adjustment" Width="620" Height="500" MinWidth="620" MinHeight="500"
        WindowStartupLocation="CenterOwner" WindowStyle="None" AllowsTransparency="False"
        Background="#F8FAFC" BorderBrush="#CBD5E1" BorderThickness="1" ResizeMode="NoResize">
    
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#1E293B"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Margin" Value="0,4,0,12"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Margin" Value="0,4,0,12"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/> <!-- Custom Drag Title Bar -->
            <RowDefinition Height="*"/>    <!-- Scrollable Content -->
            <RowDefinition Height="54"/>   <!-- Actions Bar -->
        </Grid.RowDefinitions>

        <!-- Drag Header -->
        <Border Grid.Row="0" Background="#032D60" MouseDown="TitleBar_MouseDown" Padding="16,0">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="⚖" FontSize="14" Foreground="White" Margin="0,0,8,0"/>
                    <TextBlock Text="Stock Adjustment Center" Foreground="White" FontSize="14" FontWeight="Bold"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <TextBlock Text="{Binding Product.Name}" Foreground="#9ED7FF" FontSize="11" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,16,0"/>
                    <Button Content="×" Background="Transparent" Foreground="White" BorderThickness="0" FontSize="18" Padding="0" Width="30" Height="30" Cursor="Hand" Click="Cancel_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Form Area -->
        <Border Grid.Row="1" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="20" Margin="20">
            <StackPanel>
                <Grid Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Product Code" Foreground="#64748B" FontSize="11"/>
                        <TextBlock Text="{Binding Product.Code}" FontWeight="Bold" FontSize="15" Foreground="#0F172A" Margin="0,4,0,0"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Current Stock Level" Foreground="#64748B" FontSize="11"/>
                        <TextBlock Text="{Binding Product.StockLevel, StringFormat='{}{0:0.###} units'}" FontWeight="Bold" FontSize="15" Foreground="#059669" Margin="0,4,0,0"/>
                    </StackPanel>
                </Grid>

                <TextBlock Text="Warehouse *" FontWeight="SemiBold"/>
                <ComboBox ItemsSource="{Binding Warehouses}" SelectedItem="{Binding SelectedWarehouse}" DisplayMemberPath="Name"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="14"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Adjustment Quantity *" FontWeight="SemiBold"/>
                        <TextBox Text="{Binding Quantity}" ToolTip="Positive for receipt, negative for issue."/>
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Unit Cost (TL) *" FontWeight="SemiBold"/>
                        <TextBox Text="{Binding UnitCost}"/>
                    </StackPanel>
                </Grid>

                <TextBlock Text="Reference Document / No"/>
                <TextBox Text="{Binding ReferenceNumber}"/>

                <TextBlock Text="Adjustment Reason *" FontWeight="SemiBold"/>
                <TextBox Text="{Binding Reason}" Height="54" AcceptsReturn="True" TextWrapping="Wrap" Margin="0,4,0,0" VerticalContentAlignment="Top" Padding="8,4"/>
            </StackPanel>
        </Border>

        <!-- Footer Actions Bar -->
        <Border Grid.Row="2" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="0,1,0,0" Padding="16,10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Discard" Click="Cancel_Click" Background="White" Foreground="#475569" BorderBrush="#CBD5E1" BorderThickness="1" Margin="0,0,8,0" Padding="20,0" Height="34" FontWeight="SemiBold" Cursor="Hand">
                    <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="6"/></Style></Button.Resources>
                </Button>
                <Button Content="Post Adjustment" Command="{Binding SaveCommand}" Background="#0070D2" Foreground="White" BorderThickness="0" Padding="24,0" Height="34" FontWeight="Bold" Cursor="Hand">
                    <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="6"/></Style></Button.Resources>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>

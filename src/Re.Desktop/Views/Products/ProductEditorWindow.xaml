<Window x:Class="Re.Desktop.Views.Products.ProductEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ürün Kartı Düzenle / Oluştur" Width="980" Height="700" MinWidth="980" MinHeight="700"
        WindowStartupLocation="CenterOwner" WindowStyle="None" AllowsTransparency="False"
        Background="#F8FAFC" BorderBrush="#CBD5E1" BorderThickness="1"
        UseLayoutRounding="True" SnapsToDevicePixels="True">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#1E293B"/>
            <Setter Property="FontFamily" Value="Segoe UI, Inter"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="34"/>
            <Setter Property="Margin" Value="0,4,0,10"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="34"/>
            <Setter Property="Margin" Value="0,4,0,10"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="42"/> <!-- Title Bar -->
            <RowDefinition Height="*"/>    <!-- Content Area -->
            <RowDefinition Height="58"/>   <!-- Footer Actions -->
        </Grid.RowDefinitions>

        <!-- Custom Drag Title Bar -->
        <Border Grid.Row="0" Background="#0F172A" MouseDown="TitleBar_MouseDown" Padding="16,0">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="📦 " FontSize="14" Foreground="#38BDF8" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding FormTitle}" Foreground="White" FontSize="14" FontWeight="Bold" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <TextBlock Text="ÜÜRÜN KARTI PORTALİ · RE ERP" Foreground="#94A3B8" FontSize="10.5" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,16,0"/>
                    <Button Content="✕" Background="Transparent" Foreground="White" BorderThickness="0" FontSize="16" Padding="0" Width="30" Height="30" Cursor="Hand" Click="Cancel_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Form Grid -->
        <Grid Grid.Row="1" Margin="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="240"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left: Product Image Panel -->
            <Border Grid.Column="0" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="16">
                <StackPanel>
                    <TextBlock Text="Ürün Görseli" FontWeight="Bold" FontSize="12" Foreground="#475569" Margin="0,0,0,10"/>
                    <Border Height="210" Background="#F8FAFC" BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="6" ClipToBounds="True">
                        <Grid>
                            <TextBlock Text="Görsel Seçilmedi" Foreground="#94A3B8" FontSize="11" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            <Image Source="{Binding FormModel.ImagePath}" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality"/>
                        </Grid>
                    </Border>
                    <Button Content="📷 Görsel Seç" Click="SelectImage_Click" Margin="0,16,0,8" Height="36" Background="#0070D2" Foreground="White" BorderThickness="0" FontWeight="Bold" Cursor="Hand"/>
                    <Button Content="🗑 Görseli Kaldır" Click="RemoveImage_Click" Height="34" Background="White" Foreground="#DC2626" BorderBrush="#FCA5A5" BorderThickness="1" FontWeight="SemiBold" Cursor="Hand"/>
                </StackPanel>
            </Border>

            <!-- Right: Tabbed Details -->
            <Border Grid.Column="2" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8">
                <TabControl Background="White" BorderThickness="0">
                    <TabControl.Resources>
                        <Style TargetType="TabItem">
                            <Setter Property="Foreground" Value="#64748B"/>
                            <Setter Property="Background" Value="#F8FAFC"/>
                            <Setter Property="Padding" Value="16,10"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="FontSize" Value="12.5"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Foreground" Value="#0070D2"/>
                                    <Setter Property="Background" Value="White"/>
                                    <Setter Property="BorderBrush" Value="#0070D2"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TabControl.Resources>

                    <!-- Tab 1: General Info -->
                    <TabItem Header="📋 Genel Bilgiler">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20">
                            <StackPanel>
                                <Grid>
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="16"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="Ürün Kodu *" FontWeight="Bold" FontSize="11.5" Foreground="#475569"/>
                                        <TextBox Text="{Binding FormModel.Code, UpdateSourceTrigger=PropertyChanged}"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="Kategori *" FontWeight="Bold" FontSize="11.5" Foreground="#475569"/>
                                        <ComboBox ItemsSource="{Binding Categories}" DisplayMemberPath="Name" SelectedValuePath="Id" SelectedValue="{Binding FormModel.CategoryId}"/>
                                    </StackPanel>
                                </Grid>

                                <TextBlock Text="Ürün Adı *" FontWeight="Bold" FontSize="11.5" Foreground="#475569" Margin="0,4,0,0"/>
                                <TextBox Text="{Binding FormModel.Name, UpdateSourceTrigger=PropertyChanged}"/>

                                <Grid Margin="0,4,0,0">
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="16"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="Kısa Ad" FontWeight="Bold" FontSize="11.5" Foreground="#475569"/>
                                        <TextBox Text="{Binding FormModel.ShortName, UpdateSourceTrigger=PropertyChanged}"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="Marka" FontWeight="Bold" FontSize="11.5" Foreground="#475569"/>
                                        <ComboBox ItemsSource="{Binding Brands}" DisplayMemberPath="Name" SelectedValuePath="Id" SelectedValue="{Binding FormModel.BrandId}"/>
                                    </StackPanel>
                                </Grid>

                                <TextBlock Text="Ürün Açıklaması / Detay" FontWeight="Bold" FontSize="11.5" Foreground="#475569" Margin="0,4,0,0"/>
                                <TextBox Text="{Binding FormModel.Description, UpdateSourceTrigger=PropertyChanged}" Height="70" AcceptsReturn="True" TextWrapping="Wrap"/>
                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>

                    <!-- Tab 2: Pricing & Tax -->
                    <TabItem Header="💰 Fiyat &amp; KDV">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20">
                            <StackPanel>
                                <Grid>
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="16"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="Alış Fiyatı (₺)" FontWeight="Bold" FontSize="11.5" Foreground="#475569"/>
                                        <TextBox Text="{Binding FormModel.PurchasePrice, UpdateSourceTrigger=PropertyChanged}"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="Satış Fiyatı (₺) *" FontWeight="Bold" FontSize="11.5" Foreground="#475569"/>
                                        <TextBox Text="{Binding FormModel.SalePrice, UpdateSourceTrigger=PropertyChanged}"/>
                                    </StackPanel>
                                </Grid>

                                <Grid Margin="0,4,0,0">
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="16"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="Bayi / Özel Satış Fiyatı (₺)" FontWeight="Bold" FontSize="11.5" Foreground="#475569"/>
                                        <TextBox Text="{Binding FormModel.DealerPrice, UpdateSourceTrigger=PropertyChanged}"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="KDV Oranı (%) *" FontWeight="Bold" FontSize="11.5" Foreground="#475569"/>
                                        <TextBox Text="{Binding FormModel.VatRate, UpdateSourceTrigger=PropertyChanged}"/>
                                    </StackPanel>
                                </Grid>
                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>

                    <!-- Tab 3: Stock & Unit -->
                    <TabItem Header="📏 Stok &amp; Birim">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20">
                            <StackPanel>
                                <Grid>
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="16"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="Birim *" FontWeight="Bold" FontSize="11.5" Foreground="#475569"/>
                                        <ComboBox ItemsSource="{Binding Units}" DisplayMemberPath="Name" SelectedValuePath="Id" SelectedValue="{Binding FormModel.UnitId}"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="Ana Barkod" FontWeight="Bold" FontSize="11.5" Foreground="#475569"/>
                                        <TextBox Text="{Binding PrimaryBarcodeValue, UpdateSourceTrigger=PropertyChanged}"/>
                                    </StackPanel>
                                </Grid>

                                <Grid Margin="0,4,0,0">
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="16"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="Asgari (Min) Stok Seviyesi" FontWeight="Bold" FontSize="11.5" Foreground="#475569"/>
                                        <TextBox Text="{Binding FormModel.MinStockLevel, UpdateSourceTrigger=PropertyChanged}"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="Azami (Max) Stok Seviyesi" FontWeight="Bold" FontSize="11.5" Foreground="#475569"/>
                                        <TextBox Text="{Binding FormModel.MaxStockLevel, UpdateSourceTrigger=PropertyChanged}"/>
                                    </StackPanel>
                                </Grid>

                                <StackPanel Margin="0,8,0,0">
                                    <CheckBox Content="Hızlı Satış (POS) Ekranında Göster" IsChecked="{Binding FormModel.IsPosItem}" FontSize="12.5" Margin="0,4"/>
                                    <CheckBox Content="Ürün Aktif / Satışa Açık" IsChecked="{Binding FormModel.IsActive}" FontSize="12.5" Margin="0,4"/>
                                </StackPanel>
                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>

                    <!-- Tab 4: Variants -->
                    <TabItem Header="🎨 Varyantlar">
                        <Border Padding="16">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <Button Content="+ Yeni Varyant Ekle" Command="{Binding AddVariantCommand}" HorizontalAlignment="Right" Margin="0,0,0,10" Padding="12,6" Background="#0070D2" Foreground="White" BorderThickness="0" FontWeight="Bold" Cursor="Hand"/>
                            <DataGrid Grid.Row="1" ItemsSource="{Binding Variants}" AutoGenerateColumns="False" CanUserAddRows="False" RowHeight="36" Background="White" BorderBrush="#E2E8F0" BorderThickness="1">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Varyant Kodu *" Binding="{Binding Code, UpdateSourceTrigger=PropertyChanged}" Width="1.5*"/>
                                    <DataGridTextColumn Header="Renk" Binding="{Binding Color}" Width="*"/>
                                    <DataGridTextColumn Header="Beden / Ölçü" Binding="{Binding Size}" Width="*"/>
                                    <DataGridTextColumn Header="Satış Fiyatı" Binding="{Binding SalePrice}" Width="*"/>
                                    <DataGridTemplateColumn Header="İşlem" Width="80">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Button Content="Sil" Foreground="#DC2626" Background="Transparent" BorderThickness="0" Padding="0" FontWeight="Bold" Cursor="Hand"
                                                        Command="{Binding DataContext.RemoveVariantCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </Border>
                </TabItem>
                </TabControl>
            </Border>
        </Grid>

        <!-- Footer Actions Bar -->
        <Border Grid.Row="2" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="0,1,0,0" Padding="20,10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Vazgeç" Click="Cancel_Click" Background="White" Foreground="#475569" BorderBrush="#CBD5E1" BorderThickness="1" Margin="0,0,10,0" Width="100" Height="36" FontWeight="SemiBold" Cursor="Hand"/>
                <Button Content="💾 Ürünü Kaydet" Command="{Binding SaveProductCommand}" Background="#0070D2" Foreground="White" BorderThickness="0" Width="140" Height="36" FontWeight="Bold" Cursor="Hand"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
